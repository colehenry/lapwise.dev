{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "preDeployCommand": "alembic upgrade head && PYTHONPATH=. python scripts/backfill_canonical_championships.py --missing-only --apply && PYTHONPATH=. python scripts/refresh_archive_aggregates.py"
  }
}
