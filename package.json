{
  "name": "f1-analytics",
  "private": true,
  "scripts": {
    "check": "npm run check:guardrails && npm run guardrails && npm run check:frontend && npm run check:backend",
    "check:guardrails": "node --test scripts/guardrails.test.mjs",
    "check:frontend": "npm --prefix frontend run lint && npm --prefix frontend test && npm --prefix frontend run build",
    "check:backend": "cd backend && ./venv/bin/ruff check app/ && ./venv/bin/ruff format --check . && ./venv/bin/python -m pytest tests -q",
    "guardrails": "node scripts/guardrails.mjs",
    "guardrails:update": "node scripts/guardrails.mjs --update-baseline",
    "perf:api": "node scripts/perf/measure-endpoints.mjs",
    "perf:bundle": "node scripts/perf/bundle-report.mjs",
    "perf:queries": "cd backend && ./venv/bin/python -m pytest tests/perf -q",
    "perf:requests": "npm --prefix frontend test -- test/"
  }
}
